const { createYield } = require("typescript");

describe("JSON Objects", () => {
  it("JSON Objects", () => {
    cy.openHomePage();

    const simpleObj = { key: "value", key2: "value2" };
    const simpleArr = ["one", "two", "three"];
  });
});
